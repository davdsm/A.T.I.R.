import "./index.css";

const MediaPipe = () => {
  
  // install MediaPipe in a utils file
  // execute him in that js file
  // call function like an api
  // get results from this side, but gething them on a utils js file

  return (
    <div className="MediaPipe">
      <h1> Mdia Pipe </h1>
    </div>
  );
};

export default MediaPipe;
